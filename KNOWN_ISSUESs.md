// NOTE: observed one intermittent "Invalid URL" failure on Windows when 
// running this test in isolation — unreproduced after 5 consecutive runs. 
// Suspected transient port-binding issue with supertest's ephemeral server, 
// not a code defect. Monitor if it recurs.